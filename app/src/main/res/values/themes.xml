<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base light theme -->
    <style name="Base.Theme.DietApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme colors here -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorOnPrimary">@color/my_light_on_primary</item>
        <!-- Customize other theme elements like text color, status bar color, etc. -->
    </style>

    <style name="Theme.DietApp" parent="Base.Theme.DietApp" />
</resources>
